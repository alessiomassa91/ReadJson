# Uncomment the next line to define a global platform for your project
# platform :ios, '13.0'

target 'ReadJson' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ReadJson
  pod 'Alamofire'
  pod 'AlamofireImage'
  pod 'SwiftyJSON'
  
  target 'ReadJsonTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
